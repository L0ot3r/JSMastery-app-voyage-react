import { makeStyles } from '@mui/styles'

const useStyles = makeStyles((theme) => ({

  container: {
    paddingTop: 15,
    background: '#f1f1f1',
    color: '#333',
  }

}))

export default useStyles;